import { createSlice } from "@reduxjs/toolkit";

const gptSlice =createSlice({
    name: gptSlice,
    initialState: false,
    reducers: {
        toggle: (state,action) =>{
            
        }
    }

})